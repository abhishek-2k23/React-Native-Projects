import { Link } from 'expo-router'
import { Button, Text, View } from 'react-native'

const index = () => {
  return (
    <View>
        <Text> index tab</Text>
    </View>
  )
}

export default index